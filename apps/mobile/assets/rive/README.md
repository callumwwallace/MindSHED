# Runtime Rive assets

Place the reviewed production export at `bramble.riv` after the motion-test gate
in `docs/BRAMBLE-RIG.md` passes on both iOS and Android.

Do not replace the SVG fallback with an unreviewed sample rig. The app’s current
companion state names are intentionally aligned with the production state
machine contract.
